[project]
name = "bot"
version = "0.1.0"
description = "LMS Helper Bot"
authors = [
    { name = "Student", email = "student@example.com" }
]
dependencies = [
    "aiogram>=3.17.0",
    "python-dotenv>=1.0.1",
    "httpx>=0.28.1",
    "pydantic-settings>=2.7.1",
]
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
